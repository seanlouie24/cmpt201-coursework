#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>
#include <stdlib.h>

int main() {
  pid_t pid = fork();

  if (pid == 0) {
    printf("This is child \n");
  } else {
    printf("This is parent: child pid is %d\n", pid);
  }
}
